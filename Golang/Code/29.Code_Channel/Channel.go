package main

/**
channel -> like pipe to sent data from one go routines
buffer channel -> channel who send and receive values

*/

func main(){
	// only receive data
	// un_buffer := make(chan string);

	// buffer channel can also sent data
	// can receive upto 5 data
	// buffer := make(chan string,5)

	

}
